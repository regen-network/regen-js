{
	"name": "stakebird-js",
	"version": "0.1.0",
	"author": "shane@publicawesome.com",
	"description": "Stakebird Typescript API",
	"license": "Apache-2.0",
	"private": true,
	"repository": "https://github.com/public-awesome/stakebird-js",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build:api": "yarn workspace @stakebird/api build",
		"build": "lerna run build --stream",
		"format": "yarn lint --fix",
		"lint": "tsc --noEmit && eslint --ext js,ts,tsx .",
		"start": "yarn workspace @stakebird/demo-app start",
		"test": "lerna run test"
	},
	"devDependencies": {
		"@amaurymartiny/eslintrc": "^1.3.3",
		"lerna": "^3.22.1",
		"ts-jest": "^26.4.4",
		"typescript": "^4.0.5"
	}
}
