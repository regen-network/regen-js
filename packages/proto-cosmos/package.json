{
	"name": "@regennetwork/proto-cosmos",
	"version": "0.1.0",
	"author": "admin@regen.network",
	"description": "A client library for the Regen Ledger",
	"license": "Apache-2.0",
	"main": "lib",
	"repository": "https://github.com/regen-network/regen-js",
	"scripts": {
		"build": "rimraf lib && tsc"
	},
	"devDependencies": {
		"ts-proto": "^1.39.0"
	}
}
